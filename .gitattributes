/gradlew text eol=lf
*.bat text eol=crlf
*.jar binary

# Git 줄바꿈 설정
# 모든 텍스트 파일의 줄바꿈을 LF(Unix-style)로 통일
* text=auto eol=lf

# Windows의 배치 스크립트는 CRLF(Windows-style) 유지
# cmd 파일은 대소문자 구분 없이 모두 포함 (예: .cmd, .CMD, .Cmd)
*.{cmd,[cC][mM][dD]} text eol=crlf

# bat 파일도 Windows 실행을 위해 CRLF 유지
# bat 파일은 대소문자 구분 없이 모두 포함 (예: .bat, .BAT, .Bat)
*.{bat,[bB][aA][tT]} text eol=crlf